# Launch Post Draft

Mainspring is an open-source runtime foundation for people turning agent experiments into real operator workflows.

This release focuses on the runtime spine, event journal, policy and approval flow, provider routing, a real local gateway development path, and an honest founder-cockpit console that can now preview real local gateway data instead of only static prototype state.
