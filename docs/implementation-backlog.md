# Implementation Backlog

Last updated: 2026-07-11.

| Priority | Category | Task | Rationale | Dependencies | Risk | Files/modules | Acceptance criteria | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P1 | gateway/console performance | Replace fixed full-snapshot polling with conditional revalidation | Normal operator refreshes rebuilt and transferred the full aggregate even when state was unchanged | Sanitized snapshot adapter and browser-safe client | Shared cache could retain unsafe state or miss compatibility writes | `src/gateway/LocalGateway.ts`, `src/gateway/server`, `apps/console/src/useGatewaySnapshot.ts` | `304` does not rebuild snapshot; health stays cheap; browser keeps only in-memory ETag/projection; benchmark and E2E pass | Complete |
| P1 | RunLog scheduling | Add safe bounded local worker concurrency | Independent provider-only work was needlessly serial | Fenced claims, workspace executor guard | Concurrency could duplicate work or overlap workspace side effects | `src/core/RunLogWorker.ts`, `src/core/RunLogExecutor.ts`, `src/sdk/RunLogMainspring.ts` | Default stays one; bounded claims, cancellation, workspace serialization, and deterministic throughput tests pass | Complete |
| P1 | console/operator UX | Continue decomposing the connected console | The connected shell mixed selection/commands with provider, setup, and client-editing UI | Stable gateway client and operator model | Fragmentation or state leakage during extraction | `apps/console/src/ConnectedConsoleApp.tsx`, `ConsoleNavigation.tsx`, `ConsoleProviderCatalog.tsx`, `ConsoleSetup.tsx`, `ConsoleClientForms.tsx` | Navigation, streaming activity, provider catalog/form, first-run setup, and client editing are extracted with focused tests and Playwright workflow coverage | Complete |
| P2 | console/operator UX | Improve explicit redirect/session/trace UX where RunLog semantics exist | RunLog detail state was only local React state, and compatibility event loading could race when operators changed runs quickly | Existing bounded trace route and console run selection | A stale event response could replace the newly selected run, or a URL could expose session/provider material | `apps/console/src/ConsoleLocation.ts`, `apps/console/src/useCompatibilityRunEvents.ts`, `apps/console/src/useRunLogTracePage.ts`, `apps/console/src/localGatewayClient.ts`, `apps/console/src/ConnectedConsoleApp.tsx` | Validated screen/activity/run deep links restore the selected run without sensitive URL material; canonical and compatibility trace reads are session-bound, abortable, and covered by focused tests plus real Chromium reload E2E | Complete |
| P2 | console architecture | Extract settings ownership from the connected shell | Gateway settings state and connected-service catalog rendering remained embedded in the composition shell | Provider catalog and setup state types | Feature extraction could lose gateway-draft synchronization, alter connected-service controls, or leave duplicated feature CSS | `apps/console/src/ConsoleSettings.tsx`, `ConsoleSettings.css`, `ConnectedConsoleApp.tsx` | Settings draft synchronization, connection facts, provider selection, catalog rendering, and feature-specific layout styles move behind stable props; console type/build and E2E remain green | Complete |
| P2 | console architecture | Extract shared workflow primitives from the connected shell | Dialog, activity rail, tool-toggle, and automation-flow rendering were reusable but coupled to the composition shell | Stable run/event and automation DTOs | Extraction could change interaction affordances, drag behavior, or run-event rendering | `apps/console/src/ConsoleWorkflowPrimitives.tsx`, `ConnectedConsoleApp.tsx` | Stable props preserve action rail, dialog, tool-toggle, drag metadata, and flow rendering; console type/build and E2E remain green | Complete |
| P2 | console accessibility | Harden modal keyboard behavior and narrow client tabs | The extracted dialog had no keyboard loop or focus return, and four client tabs could clip at phone widths | Shared dialog primitive and responsive console shell | Focus could escape a modal or a client surface could become unreachable on a narrow viewport | `apps/console/src/ConsoleWorkflowPrimitives.tsx`, `controlRoom.css`, `e2e/console.operator.spec.ts` | Dialogs are labelled, focus their close control, cycle Tab locally, dismiss on Escape, and restore opener focus; the 390px E2E covers every Activity tab, no document overflow, and modal focus return | Complete |
| P2 | console architecture | Extract the selected-client workspace frame | The connected shell still combined the client header, workspace context, tab navigation, and two-column layout with feature state and command orchestration | Stable client/workspace snapshot DTOs and existing responsive shell styles | The frame could lose workspace context, alter tab reachability, or create a second source of client selection state | `apps/console/src/ConsoleClientWorkspaceFrame.tsx`, `ConnectedConsoleApp.tsx`, `controlRoom.css` | A presentational frame owns client/workspace context and tab navigation through stable props; client feature behavior stays unchanged; console type/build, focused UI tests, and real Chromium E2E remain green | Complete |
| P2 | console architecture | Extract client workspace feature views from the connected shell | Chat, agent editing, automation testing, access preview, and tool configuration still lived beside gateway selection and command orchestration | Selected-client frame, stable snapshot DTOs, existing workflow primitives | Feature-local state or event wiring could drift while moving the views, or a tab could render stale client context | `apps/console/src/ConsoleClientWorkspaceViews.tsx`, `ConnectedConsoleApp.tsx`, `e2e/console.operator.spec.ts` | A feature module owns all four client views and local helpers; the shell retains only composition and gateway state; console type/build, focused UI tests, and Chromium coverage traverse Agents, Automations, Access, and Chat with a reviewed automation screenshot | Complete |
| P2 | console architecture | Split client/setup feature styling from the control-room shell | One large stylesheet mixed sidebar/activity shell rules with client, setup, chat, and automation visual rules, making feature changes harder to review safely | Existing CSS selectors and import order | A CSS boundary could change cascade order or narrow-screen reachability | `apps/console/src/controlRoom.css`, `apps/console/src/controlRoomWorkspace.css`, `ConnectedConsoleApp.tsx` | Client/setup/chat/automation rules live behind a dedicated feature stylesheet loaded after the shell; console type/build and real Chromium desktop/390px coverage remain green with no visual regression | Complete |
| P1 | gateway/console performance | Add bounded operator activity pages | Runs, approvals, and traces previously depended on the broad compatibility snapshot or full projections | Canonical RunLog projection, app-state metadata, browser DTO adapter | Cursor drift, duplicated RunLog mirrors, or browser DTO leakage | `src/core`, `src/gateway`, `apps/console/src` | Cursor-paginated RunLog and compatibility runs, merged approval history, public-only reverse trace pages, server/client/browser tests, and benchmark evidence pass | Complete |
| P2 | gateway architecture | Extract the compatibility activity reader from `LocalGateway` | The bounded page route had accumulated cache, mailbox-freshness, and cursor mechanics inside the public facade | Bounded compatibility page route and session mutation revisions | Extraction could change cursor/freshness semantics or pull the broad snapshot back into the hot path | `src/gateway/CompatibilityRunPage.ts`, `src/gateway/LocalGateway.ts` | Stable `LocalGateway.compatibilityRuns` facade delegates to a focused reader; existing mailbox/session/HTTP regression coverage proves cache, mutation, and no-snapshot behavior | Complete |
| P2 | gateway architecture | Extract aggregate snapshot and revision ownership from `LocalGateway` | The stable facade mixed command orchestration with broad aggregate projection, RunLog summary construction, and mailbox revision caching | Existing snapshot DTO and revision-cache transport | Extraction could change compatibility session handling, ETag invalidation, or worker-state summaries | `src/gateway/GatewaySnapshotReader.ts`, `src/gateway/LocalGateway.ts` | A read-only collaborator owns aggregate assembly, RunLog projection, outbox status, and revision caching; facade-level gateway and HTTP regression suites preserve snapshot, ETag, worker, and stream behavior | Complete |
| P2 | gateway architecture | Extract budget/usage derivation from `LocalGateway` | Threshold calculation, scope matching, and usage rollups were mixed with enqueue authority and transition auditing | Budget controls and usage ledger | Extraction could alter warning text, scope matching, or pre-enqueue block behavior | `src/gateway/GatewayBudgetEvaluator.ts`, `GatewayBudgetControl.ts`, `LocalGateway.ts` | Read-only evaluator owns derived status and rollups; facade retains decision/audit writes; budget and gateway regressions preserve warning/block contracts | Complete |
| P2 | gateway architecture | Extract durable projection synchronization from `LocalGateway` | Approval, usage, tool-call, artifact, execution-cell, and terminal-lease catch-up still lived in the public facade beside command orchestration | Projection-only app-state namespaces, native mailbox readers, RunLog store, HyperCell scheduler | A callback boundary could skip a source, change idempotency, or release a lease against incomplete runtime state | `src/gateway/GatewayProjectionSynchronizer.ts`, `src/gateway/LocalGateway.ts`, projection tests | A focused synchronizer owns the complete catch-up pass; native and RunLog usage remain additive/idempotent, artifact paths remain bounded, tool/cell rows stay sanitized, terminal leases use canonical event status, and existing gateway/projection suites pass | Complete |
| P2 | gateway architecture | Extract shared gateway/RunLog run-input resolution | Compatibility ingress, canonical RunLog ingress, and gateway cron preparation duplicated host binding checks for sessions, workspaces, agents, provider profiles, and runtime profiles | App-state topology, runtime session metadata, registered runtime profiles | Drift could allow an archived resource, cross-workspace agent, mismatched provider profile, or unknown runtime profile to reach a runtime | `src/gateway/GatewayRunInputResolver.ts`, `src/gateway/LocalGateway.ts`, resolver and gateway tests | One resolver maps browser-safe IDs to a host-owned runnable input, injects managed credential/model defaults, rejects binding drift before runtime enqueue, and is used by compatibility/RunLog/cron paths with focused and Chromium-backed gateway validation | Complete |
| P2 | gateway architecture | Extract cron polling lifecycle from `LocalGateway` | Timer setup, due-row selection, overlapping tick coordination, and operator status were mixed into the gateway facade beside policy/run orchestration | Gateway cron schedule store and existing cron control | A timer extraction could double-dispatch due schedules, lose errors, or change manual tick behavior | `src/gateway/GatewayCronScheduler.ts`, `src/gateway/LocalGateway.ts`, scheduler and gateway tests | A bounded scheduler owns timer start/stop, due filtering, in-flight coalescing, and last-tick/error status; policy/run behavior remains in `LocalGateway`; focused tests and Chromium-backed gateway validation pass | Complete |
| P2 | gateway architecture | Extract bounded approval-history merging from the HTTP server | The route mixed source precedence, cursor handling, and browser-safe projection with HTTP mechanics; cancelled compatibility filtering was ambiguous | Bounded approval history route and canonical RunLog approval summaries | Route behavior could change under mirrored rows, cursors, or cancelled status | `src/gateway/ApprovalHistoryPage.ts`, `src/gateway/server` | Route delegates to a focused merge service; unit and HTTP tests cover RunLog precedence, cursor continuation, `cancelled` filtering, and no-app-state fail-closed behavior | Complete |
| P2 | gateway architecture | Extract bounded canonical RunLog activity and trace reads from the HTTP server | Activity/trace routes still combined page reads, projection, and browser-event filtering with transport mechanics | Canonical RunLog read surface and console-safe DTOs | Cursor regressions or a visibility-filter failure could expose hidden event data | `src/gateway/RunLogActivityPage.ts`, `src/gateway/server` | Thin routes delegate to a focused reader; unit and HTTP tests cover cursor continuation, bounded summaries, reverse order, and a second public-only projection check | Complete |
| P2 | gateway/console performance | Add bounded detailed usage-history reads | Usage Activity could inspect only the unbounded ledger embedded in the compatibility snapshot | Gateway app-state ledger and browser-safe usage DTO | Cursor drift or metadata leakage could expose private provider details or repeat entries | `src/gateway/UsageHistoryPage.ts`, `AppStateStore.ts`, gateway server, console client/hook | `GET /usage-history` uses a reverse cursor and the indexed ledger, returns browser-safe rows without metadata, and loads only while Activity Usage is visible; the aggregate remains compatible for summaries | Complete |
| P2 | gateway/console performance | Add bounded detailed artifact, audit, and memory history reads | Artifact, audit, and memory inspection previously depended on broad aggregate projections; memory could scan every registered workspace during snapshot adaptation | App-state identifiers, browser-safe DTO adapters, workspace-local JSONL memory source | Cursor drift, path/metadata leakage, or accepting a browser-supplied host path could cross the browser boundary | `src/gateway/ArtifactHistoryPage.ts`, `AuditHistoryPage.ts`, `MemoryHistoryPage.ts`, gateway server, console client/hooks | `GET /artifact-history`, `GET /audit-history`, and workspace-required `GET /memory-history` return reverse cursors and browser-safe rows only while their Activity view is open; memory resolves its root host-side and docs retain the absence of a global index | Complete |
| P1 | gateway/console performance | Bound aggregate detail histories without losing operator totals | The broad snapshot still serialized every artifact, tool-call, usage, and audit row even though the console already loaded detailed pages on demand | Indexed app-state counts, usage rollups, cursor-paginated detail routes | A bounded tail could make dashboard totals silently undercount or leave provider/model cards incomplete | `src/gateway/GatewaySnapshotReader.ts`, `src/gateway/AppStateStore.ts`, `src/gateway/GatewayBudgetEvaluator.ts`, `src/gateway/ConsoleSnapshotAdapter.ts`, console view models | Aggregate snapshots retain only a bounded newest-first tail, exact indexed totals remain visible in console counts, provider/model usage breakdowns remain complete, detailed history routes remain unchanged, and scale/focused/E2E checks pass | Complete |
| P1 | gateway/read-model security | Restrict artifact and usage projection writes and harden artifact byte access | Artifact/usage rows were exposed as ordinary app-state mutations, artifact downloads trusted a persisted absolute path, and malformed negative usage could reduce operator budget totals | Runtime event sources, app-state read models, scoped artifact route | A forged projection row, symlink, path swap, unsafe media type, or negative ledger value could cross a host/browser or budget boundary | `src/gateway/GatewayArtifactProjection.ts`, `GatewayArtifactAccess.ts`, `GatewayUsageProjection.ts`, `AppStateStore.ts`, gateway server, `src/usage` | Public app-state artifact/usage collections are read-only; runtime writes use an explicit projection namespace; artifact IDs derive paths under the runtime root; downloads realpath-check and open before streaming; unsafe media types fall back; usage numbers are non-negative and idempotent; focused and HTTP tests pass | Complete |
| P1 | CI/CD | Make every pushed commit run canonical verification and real browser E2E | The CI workflow duplicated source checks across two matrices, did not run the real console browser flow, and the hosted release job manually repeated commands instead of invoking the canonical release script | Existing verify/release scripts and Playwright harness | Workflow drift, wasted runner time, missing browser regressions, or lost failure evidence | `.github/workflows`, `scripts/check-release-workflow.mjs`, `scripts/run-console-e2e.mjs`, package/release docs | One cross-platform verify matrix; one Chromium E2E job with retained failure evidence; pinned pnpm activation; hosted release invokes `pnpm release:check`; repository workflow verifier enforces all invariants | Complete |
| P1 | RunLog read model | Project canonical RunLog artifact outputs into gateway artifact history | Gateway artifact history indexed compatibility mailbox artifacts, but canonical RunLog tool output and artifact-created events were not materialized into the operator read model | RunLog event stream, bounded artifact projection, gateway artifact route | A provider/tool payload could smuggle a host path or a repeated sync could duplicate rows | `src/gateway/GatewayArtifactProjection.ts`, `src/gateway/GatewayProjectionSynchronizer.ts`, `src/gateway/ArtifactHistoryPage.ts`, gateway tests | RunLog artifact-created and completed-tool events are restart-safe/idempotent, only opaque IDs derive paths under the configured artifact root, host paths are discarded, and canonical rows appear in the gateway artifact history | Complete |
| P1 | browser runtime | Add an optional Playwright browser lease with trace/artifact capture | Browser tools had a safety interface but no host-owned lease implementation or durable browser artifact lifecycle | `BrowserRuntimeAdapter`, RunLog tool-session factory, configured artifact root | Stale element refs, redirects, oversized artifacts, or lease resources that outlive a run | `src/tools/PlaywrightBrowserAdapter.ts`, `src/core/RunLogExecutor.ts`, `src/sdk/RunLogMainspring.ts`, browser/runtime tests | Host-injected Playwright pages receive public-target revalidation, bounded fresh refs, root-derived screenshot/trace artifacts, `browser.lease.*`/`artifact.created` events, and release-on-attempt-end; no browser sandbox or cross-restart persistence is claimed | Complete |
| P1 | RunLog read model/operator UX | Add a durable global canonical tool-call history | The compatibility tool inventory is incomplete for production RunLog work and broad snapshot rows cannot safely represent a long-lived tool ledger | RunLog event stream, SQLite projection cursors, browser-safe DTO adapter | Replaying historical events incorrectly, leaking raw tool payloads, or presenting compatibility-only history as canonical | `src/adapters/sqlite/SqliteRunLogStore.ts`, `src/hosts/runlog/RunLogProjector.ts`, `src/gateway/RunLogToolCallHistoryPage.ts`, gateway server, console client/hook | An independent restart-safe SQLite cursor projects only fixed tool-call summaries; `GET /runlog/tool-calls` reverse-pages opaque browser rows without building the snapshot; core, HTTP, client, UI, and 390px E2E coverage pass | Complete |
| P1 | RunLog read model/operator UX | Add a durable canonical usage summary projection | Gateway usage synchronization still rescanned raw RunLog events for every refresh, while the canonical host exposed no paginated provider/model/token read model | RunLog event stream, SQLite projection cursors, gateway pricing/ledger projection | Provider attribution drift, duplicated cost rows, unbounded provider payload copying, or cursor replay gaps | `src/adapters/sqlite/SqliteRunLogStore.ts`, `src/hosts/runlog/RunLogProjector.ts`, `src/sdk/RunLogMainspring.ts`, `src/gateway/GatewayUsageProjection.ts` | `usage-summary-v1` is restart-safe and cursor-paginated, stores only normalized provider/model/token/rate-limit facts, SDK callers can list it, and gateway usage projection consumes it without rescanning raw usage events; migration, gateway, package, and release checks pass | Complete |
| P0 | security hardening | Keep security-truth guard in verify and release checks | Prevent public docs from claiming sandboxing, isolation, secure marketplace, billing, or approval-resume properties that are not implemented | None | False positives if wording is vague | `scripts/check-security-truth.mjs`, `scripts/check-release-claims.mjs`, `package.json` | `pnpm security:truth`, `pnpm verify`, and `pnpm release:check` include the guard | Complete |
| P0 | RunLog Fabric completion | Implement durable RunLog approval resume with scoped receipts | Current RunLog pauses on approval but does not resume from receipt after restart | RunLog events/checkpoints, approval receipt hashing/signing | Duplicate side effects or over-broad approval authority | `src/core`, `src/policy`, `src/adapters/sqlite`, `src/hosts/runlog` | Approve/deny/restart/mutation/replay tests pass | Complete |
| P0 | RunLog Fabric completion | Continue approved tool results back into the provider loop | The approval-resume slice needed to ask the provider for the post-tool assistant result without replaying side effects | Durable RunLog approval resume | Replaying provider/tool steps incorrectly could duplicate side effects | `src/core/RunLogExecutor.ts`, provider adapters, tests | Approved tool resume can feed a compact result into provider continuation without duplicate side effects | Complete |
| P0 | tool-policy/runtime-guard | Add canonical policy decision records for RunLog tool side effects | Need one durable policy spine beyond tool execution result status | RuntimePolicyGuard, RunLog event model | Policy bypass if wrappers call executors directly | `src/policy`, `src/tools`, `src/core` | Denied actions never reach executor; hard blocks cannot be approved; `policy.decision.recorded` appears before approval/block/complete events | Complete |
| P0 | tool-policy/runtime-guard | Extend DecisionRecord adapters to non-tool host surfaces | Channel sends, provider config mutation, artifact publish, and child-run creation need the same durable decision shape or an explicit non-authoritative classification | Canonical DecisionRecord | Parallel host wrappers could bypass policy if added without an adapter | `src/policy`, `src/sdk/RunLogMainspring.ts`, `src/gateway`, `docs/current-state.md` | Child runs and provider-profile mutations emit/persist DecisionRecords; artifact indexing is explicitly read-only; channels remain explicitly compatibility-only | Complete |
| P0 | scheduler/cron | Harden RunLog cron/headless grants | RunLog cron rows need scoped headless grants, max execution counts, mutation invalidation, and fail-closed defaults | RunLog cron rows | Cron privilege escalation or duplicate runs | `src/capabilities/cron`, `src/adapters/sqlite`, `src/core` | Dangerous cron denied by default; allowlisted grants record decisions; prompt mutation/expiry fail closed; restart no-duplicate test passes | Complete |
| P1 | scheduler/cron | Migrate gateway cron schedules onto RunLog cron policy grants | RunLog-configured gateways should use the same headless policy model as RunLog cron rows while preserving mailbox compatibility for gateways without a RunLog host | RunLog cron grants | Gateway cron could drift from canonical headless policy | `src/gateway`, `src/capabilities/cron`, `src/hosts/runlog` | Gateway cron tick/run-now creates audited RunLog runs, records cron policy decisions, denies side-effecting schedules without grants, and preserves non-RunLog mailbox compatibility | Complete |
| P1 | scheduler/cron | Add operator-facing cron grant review/create surfaces | Gateway cron can consume metadata-backed scoped grants, so operators need a public API/client path to review and create those grants without editing metadata manually | RunLog-configured gateway cron migration | Grant hashes could drift from scheduler evaluation if derived differently | `src/gateway`, `apps/console`, `src/hosts/runlog` | Operators can review schedule hash/prompt/tool scope, create expiring grants, and see grant consumption in sanitized DTOs | Complete |
| P1 | scheduler/cron | Bind gateway cron control-plane mutations and triggers to durable host decisions | Schedule configuration, grant issuance, and manual/due triggers had post-mutation audit rows or local-only attribution without one shared pre-dispatch authority seam | Gateway cron control plane and RunLog grant model | A future caller could change schedule cursor state or dispatch work without durable authority evidence, or spoof local actor attribution in hosted mode | `src/gateway/GatewayCronControl.ts`, `src/gateway/LocalGateway.ts`, `src/gateway/server`, `src/policy` | Schedule create/update/delete records `cron.schedule.write`; grant issuance records `cron.grant.create`; manual/due dispatch records `cron.trigger` before cursor mutation; trigger IDs bind RunLog metadata; all evidence is hash-only, previews are read-only, and hosted principal attribution overrides request-body actors | Complete |
| P1 | budget/operator control | Bind gateway budget configuration and warning acknowledgement to durable host decisions | Budget configuration had only post-mutation audit rows, and warning/hard-block checks did not expose a common pre-enqueue authority seam | Gateway budget rows, run budget evaluation, DecisionRecord adapter | A future caller could mutate a budget or acknowledge a warning without durable authority evidence, leak run text, or spoof hosted attribution | `src/gateway/GatewayBudgetControl.ts`, `src/gateway/server`, `src/policy` | Create/update/delete records `budget.write` before mutation; warning acknowledgement records `budget.warning.acknowledge`; hard blocks record `budget.run.block`; evidence is hash-only and hosted principal attribution overrides browser input | Complete |
| P1 | provider/security control | Bind provider-profile and managed-secret configuration to durable host decisions | Provider-profile mutations created an in-memory decision but only persisted it after app-state mutation, always under the local gateway actor | Provider profiles, managed-secret storage, DecisionRecord adapter | A crash or future caller could mutate sensitive provider configuration without preceding durable evidence, and hosted attribution could be lost or spoofed | `src/gateway/GatewayProviderProfileControl.ts`, `src/gateway/server`, `src/policy` | Create/update records `provider_config.write` before app-state mutation; audit data contains only configuration hashes; managed-secret values remain host-side; hosted principal attribution overrides browser input | Complete |
| P1 | console/operator UX | Add visual cron grant controls in the console | Operators using only the browser UI need a direct way to preview and create scoped cron grants | Cron grant API/client surface | Console controls could leak grant hashes or stale schedule context if not backed by the gateway client | `apps/console/src/App.tsx`, `apps/console/src/localGatewayClient.ts` | Console shows grant-required state, previews grant scope, creates expiring grants, and refreshes schedule DTOs with tests | Complete |
| P1 | test/audit/observability | Add local-agent security regression corpus | Convert OpenClaw/Hermes-style failure classes into permanent tests | Policy decision records | Large corpus can become noisy if expectations are vague | `src/security`, `docs/security-redteam-matrix.md` | Each failure class maps to blocked, approval, staged, documented unsafe, or deferred | Complete |
| P1 | test/audit/observability | Expand deferred local-agent security classes | The first corpus documents but does not fully solve cross-agent spoofing, subagent privilege expansion, malicious remote skill review, skill memory poisoning, and browser-adapter private-network enforcement | RunLog-native channels, subagents, staged skill review, browser lease adapter | Partial coverage can be mistaken for complete platform hardening | `src/security`, `src/hosts`, `src/capabilities`, `docs/security-redteam-matrix.md` | Child attenuation has executable coverage; remaining remote/channel/browser classes are explicit accepted limitations with owners | Complete |
| P1 | security hardening | Harden local gateway browser-origin auth boundary | The local gateway used wildcard CORS around a localhost operator lane | Current hosted-auth mode and local gateway transport | A malicious website could target localhost operator endpoints if browser-origin controls remain broad | `src/gateway/server/createLocalGatewayServer.ts`, gateway tests | CORS uses an explicit local-origin allowlist; hosted-auth bootstrap/login and preflight reject hostile browser origins while local CLI/server calls still work | Complete |
| P1 | security hardening | Require configured RunLog approval signing key outside explicit local-dev/test lanes | RunLog approval receipts fall back to a static local-dev key when no env key is configured | RunLog approval receipts | Non-local deployments could accidentally use a predictable receipt-signing key | `src/core/RunLogApprovalReceipt.ts`, `src/sdk/RunLogMainspring.ts`, docs/security checks | Configured key mode fails closed without a configured key; local-dev/test fixtures remain ergonomic and explicit | Complete |
| P1 | security hardening | Revalidate browser adapter URLs after redirects and navigation | Browser safety validated initial open targets more strongly than later browser state | Browser runtime adapter `currentUrl` support | A browser-capable run could be redirected into localhost, metadata, or private-network targets | `src/tools`, `docs/security-redteam-matrix.md` | Browser actions re-check adapter-reported current URL state after open and before/after page interaction/read operations; tests cover redirect/current-url-to-private-network cases | Complete |
| P1 | memory/skills/context | Stage and scan memory/skill/template writes | Persistent memory and skills mutate future behavior | Policy decision records, review queue | Hidden persistence or supply-chain trust collapse | `src/provenance`, `src/memory`, `src/skills`, `scripts/check-skill-provenance.mjs` | Deterministic scan and staged approve/reject tests pass; `pnpm skills:check` runs in verify and release checks | Complete |
| P1 | memory/skills/context | Add operator-facing provenance review surfaces | The review queue is code-backed and now needs public review/apply affordances for local operators | Provenance queue, public gateway surfaces | Browser DTO leakage or accidental apply without approval | `src/gateway`, `apps/console`, `src/provenance` | Operators can list, approve, reject, and apply staged memory/skill writes through public surfaces with tests | Complete |
| P1 | memory/skills/context | Add taint labels for memory and skill mutations | Deterministic scanning existed, but future context assembly needed durable provenance semantics for untrusted source material | Provenance queue, memory and skill tools | Reviewed content can still carry untrusted behavioral influence into future runs | `src/provenance`, `src/memory`, `src/skills`, `src/core` | Memory/skill records preserve source taint labels and context assembly can reason about trusted, reviewed, and untrusted material | Complete |
| P1 | memory/operator control | Add durable operator memory correction and deletion | Sanitized workspace memory history was read-only, so stale or incorrect memory could not be corrected without editing JSONL directly | Registered workspace lookup, opaque entry IDs, JSONL journal, host DecisionRecords | A browser could target raw IDs/paths, leak source metadata, or silently rewrite memory without durable evidence | `src/memory`, `src/gateway`, `apps/console/src`, `e2e` | Opaque workspace-scoped correction/deletion routes append replay-safe replacement/tombstone rows, rescan corrections, persist hash-only decision evidence/audit rows, and use confirmation dialogs with gateway, browser-safety, and 390px E2E coverage | Complete |
| P1 | provenance/operator control | Bind staged review decisions and applies to durable host authority | Gateway review operations wrote audit evidence only after mutating the JSONL review queue or memory/skill destination, and hosted requests could attribute a reviewer from the browser body | Registered workspace lookup, provenance queue, app-state audit, host DecisionRecords | Crash replay could duplicate a write; a browser reviewer could misstate hosted operator identity; decision evidence could be missing before the mutation | `src/gateway/GatewayProvenanceReviewControl.ts`, `src/provenance/ProvenanceReview.ts`, gateway server | `provenance.review.decide` and `provenance.review.apply` are persisted before queue/writer mutation with review-content hashes; hosted actor overrides request reviewer; matching memory/skill provenance recovers an interrupted journal update and terminal replay is side-effect free | Complete |
| P1 | deployment/security | Bind gateway deployment configuration and execution to durable host decisions | Deployment targets used exact confirmation and post-execution audit rows, but target mutation and driver invocation lacked one canonical pre-driver decision boundary and hosted identity was lost | Deployment drivers, app-state audit/run records, DecisionRecord adapter | A future control-plane caller could mutate a target or invoke a driver without preceding durable authority evidence, or misstate the hosted operator | `src/gateway/GatewayDeploymentControl.ts`, `src/gateway/DeploymentWizard.ts`, `src/gateway/server`, `src/policy` | Target create/update writes hash-only `deployment.target.write` authorization before app-state mutation; exact-confirmed deploy/rollback/destroy records the same target/plan-hash-bound `deployment.execute` decision before driver execution; hosted principal attribution overrides browser input and focused tests prove ordering, redaction, package, and browser surfaces | Complete |
| P1 | gateway/topology security | Bind client, workspace, agent, and runtime-session topology to durable host decisions | Topology mutations were implemented in the broad facade, audited only after writes, attributed all callers as `local-gateway`, and did not verify that an update workspace belonged to the selected client | Registered app-state topology, runtime session metadata, DecisionRecord adapter | Partial writes, cross-client workspace mutation, leaked host roots, or spoofed hosted attribution | `src/gateway/GatewayTopologyControl.ts`, `src/gateway/LocalGateway.ts`, `src/gateway/server`, `src/policy` | Client/workspace/agent/session mutations delegate to `GatewayTopologyControl`; hash-only `topology.write` authority is durable before writes; cross-client updates fail closed; linked-record deletion guards remain; hosted principal attribution overrides browser input; focused control and HTTP tests prove ordering and redaction | Complete |
| P1 | marketplace/security | Bind verified marketplace installation to durable host authority | Remote catalog replacement, template file writes, and client/workspace/agent provisioning were coordinated in the broad facade, with post-write marketplace audit and no shared hosted actor or failure evidence | Verified local/signed-remote templates, topology control, DecisionRecord adapter | A caller could replace catalogs or write files before durable authorization, lose hosted attribution, or expose raw template/root material in audit data | `src/gateway/GatewayMarketplaceControl.ts`, `src/gateway/TemplateMarketplace.ts`, `src/gateway/RemoteMarketplace.ts`, `src/gateway/server`, `src/policy` | Pinned catalog sync records hash-only `marketplace.catalog.sync` before registry replacement; local and signed-remote installs record `marketplace.install` before file writes, validate runtime profiles before mutation, pass the trusted actor through topology provisioning, record success/failure, and prove ordering/redaction in control and HTTP tests | Complete |
| P1 | gateway/run authority | Bind run ingress, cancellation, and approval responses to durable host authority | Compatibility and RunLog gateway paths wrote enqueue/approval/cancel audit only after runtime mutation, and the broad facade carried actor attribution directly into those paths | RunLog/compatibility runtime stores, app-state audit, DecisionRecord adapter | A crash or future caller could mutate run/approval state without preceding gateway evidence, cancel a run under a mismatched session, persist raw prompt/response material, or spoof hosted attribution | `src/gateway/GatewayRunControl.ts`, `src/gateway/LocalGateway.ts`, `src/gateway/server`, `src/policy` | Hash-only `run.enqueue`, `run.cancel`, and `approval.resolve` decisions are durable before lifecycle mutation; run/session and approval/run/session bindings fail closed; outcome/failure rows retain the decision link without raw request contents; hosted identity overrides browser input; focused control, gateway, and HTTP tests prove ordering and redaction | Complete |
| P1 | RunLog Fabric completion | Record RunLog canonicalization and legacy retirement map | The repo needed a tested source-grounded map before removing mailbox/RuntimeKernel paths | Current SDK/gateway compatibility audit | Deleting legacy paths too early would break public SDK/gateway behavior | `docs/migration-runlog.md`, `scripts/check-runlog-migration.mjs`, `package.json` | Migration table covers legacy modules and `pnpm runlog:migration:check` runs in verify/release | Complete |
| P1 | RunLog Fabric completion | Add RunLog-native SDK host | New SDK code needs direct `RunIntent` creation and `RunLogProjection` reads before the gateway/console migration | Scoped signed RunLog receipt handling, projection compatibility, migration map | Breaking existing `createMainspring` users if old factory is flipped too early | `src/sdk/RunLogMainspring.ts`, `src/hosts/runlog`, `src/core` | SDK provider-only run, pending approval projection, and approved-tool completion tests pass | Complete |
| P1 | RunLog Fabric completion | Move OpenRouter live-provider E2E to RunLog | The optional live provider verifier was the remaining live proof tied to the mailbox runtime spine | RunLog SDK host, OpenRouterProvider, secret redaction checks | Live verifier could leak credential refs or become mandatory in release checks | `scripts/openrouter-e2e.mjs`, `scripts/check-optional-verifiers.mjs`, `docs/operations.md`, `docs/migration-runlog.md` | `pnpm openrouter:e2e` uses `createRunLogMainspring`; missing-key diagnostics are machine-readable and release-safe; live run passes when credentials/network exist | Complete |
| P1 | docs/release polish | Add RunLog-native provider example and quickstart | README and examples needed a runnable `createRunLogMainspring` front door while compatibility examples remain for mailbox-only behaviors | RunLog SDK host | Example docs could imply all examples are RunLog-native when several still exercise compatibility | `examples/provider-run`, `README.md`, `docs/examples.md`, `docs/getting-started.md`, `package.json` | `pnpm example:provider-run`, `pnpm examples:check`, and `agentic:check` exercise a provider-only RunLog run with no paid key | Complete |
| P1 | docs/release polish | Add RunLog-native tool approval example | Public examples need to prove the canonical SDK host can pause for approval, issue a scoped receipt, resume ToolRegistry execution, and complete provider continuation | RunLog SDK host, scoped RunLog receipts, file tools | Example docs must not overstate host protection | `examples/tool-approval`, `package.json`, `scripts/check-agentic-harness.mjs`, `docs/examples.md` | `pnpm example:tool-approval`, `pnpm examples:check`, and `agentic:check` exercise approval-gated `file.write` through RunLog | Complete |
| P1 | docs/release polish | Move coding-agent example to RunLog | The coding walkthrough can prove a read-then-approved-write workspace flow on the canonical host instead of the compatibility mailbox runtime | RunLog SDK host, scoped RunLog receipts, default file tools | Example docs could imply shell execution, test running, patch artifacts, or protection beyond the local file workflow shown | `examples/coding-agent`, `docs/examples.md`, `docs/getting-started.md`, `docs/migration-runlog.md` | `pnpm example:coding-agent`, `pnpm examples:check`, and `agentic:check` exercise a RunLog `file.read` plus approval-gated `file.write` run with policy decisions and checkpoint projection | Complete |
| P1 | docs/release polish | Move personal-assistant example to RunLog | The personal local-assistant walkthrough should prove read-only workspace tools on the canonical host rather than the compatibility mailbox runtime | RunLog SDK host and default file tools | Example docs could imply host file reads are a security boundary or that memory/browser behavior is covered | `examples/personal-assistant`, `docs/examples.md`, `docs/getting-started.md`, `docs/migration-runlog.md` | `pnpm example:personal-assistant`, `pnpm examples:check`, and `agentic:check` exercise a RunLog `file.read` run with policy decision and checkpoint projection | Complete |
| P1 | docs/release polish | Move support-agent example to RunLog | The support walkthrough is a read-only FAQ workflow that should not require the compatibility mailbox runtime | RunLog SDK host and default file tools | Example docs could imply customer-message sending or external system writes are implemented in this no-key walkthrough | `examples/support-agent`, `docs/examples.md`, `docs/getting-started.md`, `docs/migration-runlog.md` | `pnpm example:support-agent`, `pnpm examples:check`, and `agentic:check` exercise a RunLog `file.read` run with policy decision and checkpoint projection | Complete |
| P1 | docs/release polish | Move agency-client-agent example to RunLog | The client deliverable walkthrough can prove a read-then-approved-write workflow on the canonical host instead of the compatibility mailbox runtime | RunLog SDK host, scoped RunLog receipts, default file tools | Example docs could imply customer delivery integrations or protection beyond an approval-gated local workspace write | `examples/agency-client-agent`, `docs/examples.md`, `docs/getting-started.md`, `docs/migration-runlog.md` | `pnpm example:agency-client-agent`, `pnpm examples:check`, and `agentic:check` exercise a RunLog `file.read` plus approval-gated `file.write` run with policy decisions and checkpoint projection | Complete |
| P1 | docs/release polish | Move local-first-agent example to RunLog | The local-first walkthrough should prove approval-gated `memory.write`, provenance scanning, and `memory.read` on the canonical host instead of the compatibility mailbox runtime | RunLog SDK host, scoped RunLog receipts, memory tools | Example docs could imply memory writes are automatically safe or that memory retrieval is already wired into model context assembly | `examples/local-first-agent`, `docs/examples.md`, `docs/getting-started.md`, `docs/migration-runlog.md` | `pnpm example:local-first-agent`, `pnpm examples:check`, and `agentic:check` exercise a RunLog approval-gated `memory.write` plus `memory.read` run with policy decisions and checkpoint projection | Complete |
| P1 | RunLog Fabric completion | Add explicit gateway RunLog intake endpoints | Gateway/API consumers need a RunLog public lane before the default legacy route is flipped | RunLog SDK host, scoped signed RunLog receipt handling, projection compatibility | Breaking existing gateway DTOs if `/runs/start` changes too early | `src/gateway/LocalGateway.ts`, `src/gateway/server/createLocalGatewayServer.ts`, `src/hosts/runlog` | `/runlog/runs/start`, `/runlog/runs/:runId/events`, and `/runlog/approvals/:approvalId/resolve` tests pass | Complete |
| P1 | RunLog Fabric completion | Make default gateway run creation use RunIntent | RunLog should become canonical public ingress beyond the explicit `/runlog` lane | Explicit RunLog gateway route, migration map | Breaking existing gateway DTOs and examples | `src/gateway`, `src/storage`, `src/hosts/runlog` | Existing `/runs/start` DTO tests pass while backed by RunLog when a RunLog host is configured; non-RunLog gateways keep mailbox compatibility | Complete |
| P1 | RunLog Fabric completion | Configure local gateway dev server with a RunLog host | The normal local bring-up path should exercise the canonical RunLog default route, not only isolated tests | RunLog SDK host, default gateway RunIntent route | Dev provider fallback or provider-profile secrets could drift from docs | `src/gateway/server/dev.ts`, `docs/features/local-gateway.md` | `pnpm gateway:dev:help`, focused gateway tests, and release checks pass; no-key local dev falls back to EchoProvider | Complete |
| P1 | RunLog Fabric completion | Add RunLog provider secret resolution for managed provider profile secrets | RunLog-backed gateway starts need parity with the compatibility runtime's host-side provider secret resolver | RunLog provider query context and gateway secret resolver | Provider keys or managed secret references could leak if passed through browser DTOs incorrectly | `src/sdk/RunLogMainspring.ts`, `src/core/RunLogExecutor.ts`, `src/gateway` | Managed/non-env provider profile secret refs can be resolved by RunLog provider calls without renderer localStorage or raw secret exposure | Complete |
| P1 | console/operator UX | Read RunLogProjection in console/gateway surfaces | Operators need one event projection, not duplicate read models | SDK/gateway RunLog ingress | Browser DTO leakage if projection is not sanitized | `src/gateway`, `apps/console/src`, `src/hosts/runlog` | Gateway snapshot, console adapter, data-source, and dashboard tests use sanitized RunLog projection safely | Complete |
| P1 | console/operator UX | Expose RunLog checkpoint and error summaries in run-detail projections | Public run inspection should not require scanning raw event payloads to see checkpoints or failures | RunLogProjection, sanitized gateway/console DTOs | Browser DTO leakage if checkpoint/error payloads are exposed directly | `src/hosts/runlog`, `src/gateway`, `apps/console/src` | RunLog projections include checkpoint/error summaries; browser-facing DTOs redact path-like text and omit raw checkpoint/error payloads | Complete |
| P1 | console/operator UX | Add selected-client RunLog detail panels | Operators need visible checkpoint, policy, error, tool-call, and artifact summaries after the DTO projection exists | Sanitized RunLog console DTOs | UI could imply replay controls exist if labels are too broad | `apps/console/src` | Selected-client detail shows RunLog runs, checkpoints, policy decisions, errors, tool-call counts, and artifact counts without raw event payloads | Complete |
| P2 | docs/release polish | Add research synthesis and review artifacts | Goal requires external lessons tied to Mainspring choices | Baseline source inspection and web/source research | Generic survey fluff if not tied to code gaps | `docs/research`, `docs/reviews` | Matrix, threat model, backlog, bibliography, accepted/rejected findings | Complete |
| P2 | docs/release polish | Refresh GitHub README/SECURITY/roadmap after code hardening | Public first impression must match implemented state | Security truth guard | Docs can outpace code if updated too early | `README.md`, `SECURITY.md`, `docs/roadmap.md` | Guarded by `pnpm security:truth`, docs check, and the passing release gate | Complete |
| P2 | docs/release polish | Add lightweight SVG brand assets/theme | GitHub-ready polish without binary bloat | None | Cosmetic work can distract from runtime truth | `assets/brand`, optional `src/theme` | SVG assets exist and package surface remains clean | Complete |
| P2 | security hardening | Add hosted gateway operator roles and user administration | Authenticated hosted sessions previously had identical authority | Hosted auth and app-state users | A read-only account could mutate runtime or security configuration if route classification drifted | `src/gateway/server`, `src/gateway/AppStateStore.ts`, auth tests | Admin/operator/viewer roles are persisted; authorization runs before body parsing; unknown mutations fail closed to admin; sanitized admin user APIs protect the final active admin and revoke sessions on credential changes | Complete |
| P2 | security hardening | Add signed remote template catalogs and safe installs | Local repo templates did not provide a verifiable remote distribution path | Publisher key pinning, HTTPS target policy, local template constraints | Catalog tampering, SSRF, executable payloads, prompt injection, path/symlink escape, stale trust | `src/gateway/RemoteMarketplace.ts`, marketplace gateway/server/client, verifier | Ed25519 signatures, pinned publisher/key identity, validity windows, bounded catalogs, redirect target revalidation, deterministic provenance scans, markdown/JSON-only hash-checked files, atomic cache replacement, collision rejection, audited sync/install, and pre-existing symlink traversal rejection are tested | Complete |
| P2 | deployment | Add a guarded Kubernetes gateway deployment driver | Container/VPS deployment did not provide a cluster apply/rollout path | Gateway image, kubectl context, registry, existing namespace/Secret/PVC | Wrong context, secret leakage, SQLite replica corruption, destructive cleanup, weak pod security | `src/gateway/KubernetesDeploymentDriver.ts`, `deploy/kubernetes`, deploy verifier | Validated config; release-specific image; namespace/Secret/PVC preflight; single-replica hardened generated manifest; server-side apply; rollout/rollback; opt-in destroy preserving Secret/PVC; trusted browser origins; tests and built verifier | Complete |
| P3 | deferred/rejected | Hosted billing, paid marketplace/reputation service, production cluster certification/operations, VM isolation | These capabilities require external product/platform infrastructure and remain forbidden as fake product surfaces | Real infrastructure, implementations, and tests | Severe claim drift | Docs and release guards | Mention only as not implemented/future work | Deferred |
