# Personal Assistant Policy

- Allow reading files inside the personal workspace.
- Allow memory reads and writes for durable notes.
- Require approval before shell execution, file mutation, browser form submission, or paid API work.
- Treat external publishing and message sending as operator-reviewed actions.
