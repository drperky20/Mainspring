# Tool Approval Policy

- `file.write` is the only exposed tool.
- Workspace mutation requires approval.
- Approval is scoped to the exact RunLog request snapshot and expires quickly.
- Host file execution is not containment.
