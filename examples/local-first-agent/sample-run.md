# Sample Run

- Create one local-only workspace and session.
- Run through the mailbox-backed runtime loop.
- Resolve one approval-backed `memory.write` through the standard approval API.
- Read the stored memory back through `memory.read` and print a JSON summary.
