# Sample Run

- Create one local-only workspace and session.
- Start one `RunIntent` through `createRunLogMainspring`.
- Resolve one approval-backed `memory.write` through a scoped RunLog receipt.
- Read the stored memory back through `memory.read` and print a JSON summary.
