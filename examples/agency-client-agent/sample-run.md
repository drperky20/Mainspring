# Sample Run

- Create one client workspace for an agency-managed operator.
- Read one launch brief from the client workspace.
- Resolve one approval-backed `file.write` through the standard approval API.
- Produce one client-update deliverable and print a JSON run summary.
