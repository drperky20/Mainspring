# Coding Agent Policy

- Permit workspace-contained reads by default.
- Require approval before shell execution and broad file mutation.
- Keep generated changes reviewable through run traces and receipts.
- Do not treat shell access as sandboxed containment.
